import 'package:flutter_omarchy/flutter_omarchy.dart';

class OmarchyInput extends StatelessWidget {
  const OmarchyInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
