import 'package:flutter/widgets.dart';

class OmarchyLoader extends StatelessWidget {
  const OmarchyLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
