import 'package:flutter/widgets.dart';

class OmarchyStatusBar extends StatelessWidget {
  const OmarchyStatusBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
