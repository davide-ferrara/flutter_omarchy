import 'package:flutter/widgets.dart';

class OmarchyCheckbox extends StatelessWidget {
  const OmarchyCheckbox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
